package com.plot.plugin.powerline.preview.overlay;

/** 画布塔位标记来源（解释性语义，非持久化 Shape）。 */
public enum MarkerSource {
    AUTO_LAYOUT,
    USER_OVERRIDE,
    TERRAIN_AUTO_INSERT,
    ENDPOINT,
    CORNER,
    STANDALONE
}
