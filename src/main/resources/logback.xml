<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出配置 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>[%d{HH:mm:ss}] [%thread/%level] (%logger{0}) %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 文件输出配置（可选，用于调试） -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/masterplanner.log</file>
        <encoder>
            <pattern>[%d{yyyy-MM-dd HH:mm:ss}] [%thread/%level] (%logger) %msg%n</pattern>
        </encoder>
    </appender>

    <!-- MasterPlanner 模块的日志级别控制 -->
    <logger name="MasterPlanner" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>
    
    <!-- 详细的子模块日志级别控制 -->
    <!-- 方块分类管理器 - 生产环境建议使用 WARN -->
    <logger name="MasterPlanner/BlockCategoryManager" level="WARN" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>
    
    <!-- 方块图标渲染器 - 生产环境建议使用 INFO -->
    <logger name="MasterPlanner/BlockIconRenderer" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>
    
    <!-- 初始化器 - 保持 INFO 级别以监控启动过程 -->
    <logger name="MasterPlanner/MasterPlannerInitializer" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>
    
    <!-- 画布核心 - 生产环境建议使用 WARN -->
    <logger name="MasterPlanner/CanvasCore" level="WARN" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>
    
    <!-- 绘图工具 - 生产环境建议使用 WARN -->
    <logger name="MasterPlanner/DrawingTool" level="WARN" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>
    
    <!-- 选择管理 - 生产环境建议使用 WARN -->
    <logger name="MasterPlanner/Selection" level="WARN" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- 开发模式：启用详细调试日志 -->
    <!-- 取消注释以下配置以启用调试模式 -->
    <!--
    <logger name="MasterPlanner/BlockCategoryManager" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>
    
    <logger name="MasterPlanner/BlockIconRenderer" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>
    -->

    <!-- 填充工具相关类的详细调试日志 -->
    <logger name="com.masterplanner.ui.tools.impl.modify.FillTool" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>
    
    <logger name="com.masterplanner.ui.tools.impl.modify.strategy.FillWithSelectionStrategy" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>
    
    <logger name="com.masterplanner.ui.tools.impl.modify.helper.FillHandler" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>
    
    <logger name="com.masterplanner.ui.tools.impl.modify.helper.BoundaryDetectionHelper" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>
    
    <logger name="com.masterplanner.ui.utils.GeometryCalculationUtils" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </logger>
    
    <!-- 根日志级别 -->
    <root level="DEBUG">
        <appender-ref ref="CONSOLE"/>
    </root>
    
    <!-- 系统属性控制的动态日志级别 -->
    <!-- 
    使用方法：
    1. 在启动时设置系统属性: -Dmasterplanner.debug=true
    2. 或在游戏中设置: System.setProperty("masterplanner.debug", "true")
    -->
    <if condition='property("masterplanner.debug").equals("true")'>
        <then>
            <!-- 调试模式：所有 MasterPlanner 日志设为 DEBUG 级别 -->
            <logger name="MasterPlanner" level="DEBUG" additivity="false">
                <appender-ref ref="CONSOLE"/>
                <appender-ref ref="FILE"/>
            </logger>
        </then>
    </if>
    
    <if condition='property("masterplanner.trace").equals("true")'>
        <then>
            <!-- 追踪模式：所有 MasterPlanner 日志设为 TRACE 级别 -->
            <logger name="MasterPlanner" level="TRACE" additivity="false">
                <appender-ref ref="CONSOLE"/>
                <appender-ref ref="FILE"/>
            </logger>
        </then>
    </if>
</configuration> 